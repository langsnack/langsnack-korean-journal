# Superseded Notification Setup

The Wix Automation instructions in the earlier version are no longer used.

Use:

`WIX_DIRECT_REVIEW_EMAIL_SETUP.md`

The V6 notification flow uses `triggeredEmails.emailMember()` directly from backend code.
